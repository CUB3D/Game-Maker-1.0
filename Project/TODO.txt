TODO:
add key + mouse listener button + pre made class
ability to add maps?

way of viewing variables

add animated sprites

add file view, show floders and files if file is clicked show contents of that file, if script is clicked show edit vars and have button for edit scripts also show sprites as there own file and prefabs as there own file

CTRL + S for saving

add drag x only and drag y only handles
Right click > edit: opens a menu with settings like the add sprite but edits an existing one